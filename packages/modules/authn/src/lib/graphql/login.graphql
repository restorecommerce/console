mutation Login($input: IIoRestorecommerceUserLoginRequest!) {
  identity {
    user {
      Login(input: $input) {
        details {
          status {
            code
            message
          }
          payload {
            id
            name
            email
            active
            firstName
            lastName
          }
        }
      }
    }
  }
}
