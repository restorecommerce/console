query IdentityUserFind($input: IIoRestorecommerceUserFindRequest!) {
  identity {
    user {
      Find(input: $input) {
        details {
          operationStatus {
            code
            message
          }
          items {
            payload {
              id
              active
              activationCode
              email
              name
              firstName
              lastName
              defaultScope
              localeId
              timezoneId
              userType
              roleAssociations {
                role
              }
            }
          }
        }
      }
    }
  }
}
